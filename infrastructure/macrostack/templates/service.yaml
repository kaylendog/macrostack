apiVersion: v1
kind: Service
metadata:
  name: {{ include "macrostack.fullname" . }}
  labels:
    {{- include "macrostack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "macrostack.selectorLabels" . | nindent 4 }}
