apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "macrostack.fullname" . }}-test-connection"
  labels:
    {{- include "macrostack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "macrostack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
